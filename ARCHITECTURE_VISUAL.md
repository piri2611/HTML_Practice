# Architecture Visual Guide

## 📊 Application Architecture Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER INTERFACE                          │
│                        (Browser / React)                        │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         │ User Interactions
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                      FRONTEND LAYER                             │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  Components (src/frontend/components/)                   │  │
│  │  ├─ Auth.tsx                                             │  │
│  │  ├─ Dashboard.tsx                                        │  │
│  │  ├─ Quiz.tsx                                             │  │
│  │  ├─ StudyArea.tsx                                        │  │
│  │  ├─ ProgressDashboard.tsx                                │  │
│  │  └─ HTMLTagsReference.tsx                                │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  Context (src/frontend/context/)                         │  │
│  │  └─ AuthContext.tsx (Global State)                       │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  Utils (src/frontend/utils/)                             │  │
│  │  ├─ syntaxHighlight.ts                                   │  │
│  │  └─ htmlTagsReference.ts                                 │  │
│  └──────────────────────────────────────────────────────────┘  │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         │ BackendAPI calls
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                    BACKEND API LAYER                            │
│                  (src/backend/api/index.ts)                     │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  BackendAPI                                              │  │
│  │  ├─ questions.*                                          │  │
│  │  ├─ quiz.*                                               │  │
│  │  ├─ storage.*                                            │  │
│  │  ├─ supabase                                             │  │
│  │  └─ dbConfig                                             │  │
│  └──────────────────────────────────────────────────────────┘  │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         │ Service calls
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                     SERVICE LAYER                               │
│                  (src/backend/services/)                        │
│  ┌────────────────┐  ┌────────────────┐  ┌─────────────────┐  │
│  │ QuestionService│  │  QuizService   │  │ StorageService  │  │
│  ├────────────────┤  ├────────────────┤  ├─────────────────┤  │
│  │ • getAll()     │  │ • saveProgress │  │ • markSolved()  │  │
│  │ • getById()    │  │ • saveResult   │  │ • getSolved()   │  │
│  │ • getBy        │  │ • saveAttempt  │  │ • clearSolved() │  │
│  │   Difficulty() │  │ • getUserStats │  │ • isSolved()    │  │
│  │ • checkAnswer()│  │ • getUserResults│  │                 │  │
│  │ • calculate    │  │                │  │                 │  │
│  │   Score()      │  │                │  │                 │  │
│  └────────┬───────┘  └────────┬───────┘  └────────┬────────┘  │
│           │                   │                    │            │
│           └───────────────────┴────────────────────┘            │
│                               │                                 │
└───────────────────────────────┼─────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│                   DATABASE / STORAGE LAYER                      │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  Supabase Database                                       │  │
│  │  ├─ questions table                                      │  │
│  │  ├─ question_blanks table                                │  │
│  │  ├─ user_progress table                                  │  │
│  │  ├─ quiz_results table                                   │  │
│  │  ├─ quiz_attempts table                                  │  │
│  │  └─ profiles table                                       │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  Local Storage (Browser)                                 │  │
│  │  └─ solved questions cache                               │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Data Flow Example: Loading Questions

```
User Opens Study Area
        │
        ▼
┌────────────────────┐
│  StudyArea.tsx     │
│  Component renders │
└────────┬───────────┘
         │
         │ useEffect(() => {
         │   loadQuestions();
         │ }, []);
         ▼
┌────────────────────────────┐
│ BackendAPI.questions       │
│   .getAll()                │
└────────┬───────────────────┘
         │
         ▼
┌────────────────────────────┐
│ QuestionService            │
│   .getAllQuestions()       │
└────────┬───────────────────┘
         │
         │ 1. Fetch from DB
         │ 2. Join with blanks
         │ 3. Format response
         ▼
┌────────────────────────────┐
│ Supabase Database          │
│  SELECT * FROM questions   │
│  JOIN question_blanks      │
└────────┬───────────────────┘
         │
         │ Returns: QuestionData[]
         ▼
┌────────────────────────────┐
│ Component updates state    │
│ setQuestions(data)         │
└────────┬───────────────────┘
         │
         ▼
┌────────────────────────────┐
│ UI Re-renders with data    │
└────────────────────────────┘
```

---

## 📝 Data Flow Example: Submitting Quiz

```
User Submits Quiz
        │
        ▼
┌─────────────────────────┐
│  Quiz.tsx               │
│  handleSubmit()         │
└────────┬────────────────┘
         │
         │ Step 1: Calculate score
         ▼
┌────────────────────────────────┐
│ BackendAPI.questions           │
│   .calculateScore(correct, total) │
└────────┬───────────────────────┘
         │
         │ Returns: percentage
         ▼
┌─────────────────────────┐
│  Quiz.tsx               │
│  Step 2: Save progress  │
└────────┬────────────────┘
         │
         ▼
┌────────────────────────────────┐
│ BackendAPI.quiz                │
│   .saveProgress(...)           │
└────────┬───────────────────────┘
         │
         ▼
┌────────────────────────────────┐
│ QuizService                    │
│   .saveUserProgress(...)       │
└────────┬───────────────────────┘
         │
         │ INSERT/UPDATE
         ▼
┌────────────────────────────────┐
│ Supabase Database              │
│  UPDATE user_progress          │
│  SET question_X_status = '...' │
└────────┬───────────────────────┘
         │
         │ Success
         ▼
┌─────────────────────────┐
│  Quiz.tsx               │
│  Step 3: Mark solved    │
└────────┬────────────────┘
         │
         ▼
┌────────────────────────────────┐
│ BackendAPI.storage             │
│   .markSolved(userId, qId)     │
└────────┬───────────────────────┘
         │
         ▼
┌────────────────────────────────┐
│ StorageService                 │
│  localStorage.setItem(...)     │
└────────┬───────────────────────┘
         │
         │ Saved locally
         ▼
┌────────────────────────────────┐
│ UI shows success message       │
└────────────────────────────────┘
```

---

## 🏗️ Component Dependency Graph

```
App.tsx
  │
  ├─► AuthProvider (Context)
  │     │
  │     └─► Wraps entire app
  │
  └─► AppContent
        │
        ├─► Auth.tsx
        │     ├─ Uses: useAuth()
        │     └─ Calls: BackendAPI.supabase
        │
        ├─► Dashboard.tsx
        │     ├─ Uses: useAuth()
        │     ├─ Calls: BackendAPI.questions.getAll()
        │     └─ Calls: BackendAPI.supabase
        │
        ├─► Quiz.tsx
        │     ├─ Uses: useAuth()
        │     ├─ Calls: BackendAPI.questions.getById()
        │     ├─ Calls: BackendAPI.quiz.saveProgress()
        │     └─ Calls: BackendAPI.storage.markSolved()
        │
        └─► StudyArea.tsx
              ├─ Uses: useAuth()
              ├─ Calls: BackendAPI.questions.getAll()
              └─► Includes: HTMLTagsReference.tsx
```

---

## 📦 Module Organization

```
src/
├── backend/                         # Business Logic Layer
│   ├── api/
│   │   └── index.ts                # ← Single Entry Point
│   │       ├─ Exports: BackendAPI
│   │       ├─ Exports: Types
│   │       └─ Aggregates: All Services
│   │
│   ├── services/                    # Service Layer
│   │   ├── QuestionService.ts      # ← Question operations
│   │   ├── QuizService.ts          # ← Quiz operations
│   │   └── StorageService.ts       # ← Storage operations
│   │
│   ├── config/                      # Configuration Layer
│   │   └── database.ts             # ← DB connection
│   │
│   └── types/                       # Type Definitions
│       └── index.ts                # ← Shared types
│
└── frontend/                        # Presentation Layer
    ├── components/                  # UI Components
    │   ├── Auth.tsx
    │   ├── Dashboard.tsx
    │   ├── Quiz.tsx
    │   ├── StudyArea.tsx
    │   ├── ProgressDashboard.tsx
    │   └── HTMLTagsReference.tsx
    │
    ├── context/                     # State Management
    │   └── AuthContext.tsx
    │
    ├── utils/                       # Frontend Utilities
    │   ├── syntaxHighlight.ts
    │   └── htmlTagsReference.ts
    │
    └── index.ts                     # Frontend exports
```

---

## 🎯 Key Design Principles

### 1. Single Responsibility
```
Each service has ONE job:
├─ QuestionService  → Handles questions
├─ QuizService      → Handles quiz logic
└─ StorageService   → Handles local storage
```

### 2. Dependency Injection
```
Frontend depends on BackendAPI (abstraction)
    ↓
NOT on specific services (implementation)
```

### 3. Separation of Concerns
```
Frontend:  What to show
Backend:   How to get data
Database:  Where to store data
```

### 4. Type Safety
```
All data flows through typed interfaces:
Frontend ← TypeScript Types → Backend
```

---

## 🚀 Benefits Visualization

```
┌──────────────────┐
│   Old Structure  │
│                  │
│  Components      │
│       ↕          │
│  Direct DB calls │
│       ↕          │
│  Supabase        │
│                  │
│  ❌ Tightly      │
│     coupled      │
└──────────────────┘

┌──────────────────┐
│  New Structure   │
│                  │
│  Components      │
│       ↕          │
│  BackendAPI      │ ← Single interface
│       ↕          │
│  Services        │ ← Business logic
│       ↕          │
│  Database        │
│                  │
│  ✅ Loosely      │
│     coupled      │
│  ✅ Testable     │
│  ✅ Maintainable │
└──────────────────┘
```
