# Building the Project

## Prerequisites
- Node.js 18+ installed
- npm package manager

## Installation

```bash
npm install
```

## Development

```bash
npm run dev
```

This starts a development server at http://localhost:3005 (or next available port)

## Production Build

```bash
npm run build
```

This creates an optimized production build in the `dist/` folder.

## Preview Production Build

```bash
npm run preview
```

This previews the production build locally.

## Project Structure

- `src/` - Source code (TypeScript)
  - `backend/` - Backend services and API layer
  - `frontend/` - React components and UI
  - `App.tsx` - Main application component
  - `main.tsx` - Application entry point

- `dist/` - Production build output (generated by `npm run build`)

## Documentation

- `ARCHITECTURE.md` - Complete architecture documentation
- `BACKEND_API_REFERENCE.md` - Backend API quick reference
- `ARCHITECTURE_VISUAL.md` - Visual architecture diagrams
- `MIGRATION_SUMMARY.md` - Implementation details

## Troubleshooting

### "Cannot find module" errors
Run `npm install` again to ensure all dependencies are properly installed

### Build fails
- Clear node_modules: `rm -r node_modules && npm install`
- Clear dist folder: `rm -r dist && npm run build`

### Development server won't start
- Check if port 3000-3005 are available
- Try: `npm run dev -- --port 5173`
